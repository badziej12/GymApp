import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const trainingSlice = createSlice({
    name: 'training',
    initialState,
    reducers: {
        // Define your reducers here
    }
})